{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'No frontend build step required'",
        "dev": "vite"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
